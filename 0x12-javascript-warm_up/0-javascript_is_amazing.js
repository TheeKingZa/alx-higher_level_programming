#!/usr/bin/node
// The above line is called a shabang. it indicates that the the script should be run using the specified interpreter,
// in this case, the 'node' interpreter.

// Declare a constant variable 'myVar' and assign it the string value 'JavaScript is amazing'.
const myVar = 'JavaScript is amazing';

// Print the Value of 'myVar' to Console.
console.log(myVar);
